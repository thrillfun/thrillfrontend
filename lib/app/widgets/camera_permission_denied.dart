import 'package:flutter/material.dart';

class CameraPermissionDenied extends StatelessWidget {
  const CameraPermissionDenied({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
